#include <iostream> 
using namespace std; 
int remainder(int num, int divisor) { 
return (num - divisor * (num / divisor)); 
} 
int main() { 
  int a=0;
  int b=0;
  cin>>a;
  cin>>b;
	cout <<"The remainder of"<<a<<"/"<<b<<"is"<< remainder(a, b)<<" "<<endl; 
	return 0; 
} 
